<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.ac.plymouth.android.tutorialhelp">

    <!-- <uses-permission -->
    <!-- android:required="true" -->
    <!-- android:name="android.permission.ACCESS_WIFI_STATE"/> -->
    <!-- <uses-permission -->
    <!-- android:required="true" -->
    <!-- android:name="android.permission.CHANGE_WIFI_STATE"/> -->
    <!-- <uses-permission -->
    <!-- android:required="true" -->
    <!-- android:name="android.permission.INTERNET"/> -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.nearby.messages.API_KEY"
            android:value="AIzaSyBY7NKepHh39AszF92Y6IHZpecUMZb-aes" />

        <activity android:name=".MainActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:theme="@style/AppTheme" />
        <activity android:name=".SessionListActivity" />
        <activity android:name=".SessionLoginActivity" />
        <activity android:name=".RequestHelpActivity" />
        <activity android:name=".RequestQueueActivity" />
        <activity android:name=".CreateSessionActivity" />
        <activity android:name=".CurrentRequestActivity" />
        <activity android:name=".SelectReferActivity"></activity>
    </application>

</manifest>